# Fullchee public notes

## Build (to debug)
- `npx quartz build`

Uses [Quartz](https://quartz.jzhao.xyz/)


Setup the upstream remote to get the latest changes

Fetch the latest from the upstream repo with `git pull upstream v4`
